
if (project.hasProperty('enableDexcount')) {
    apply plugin:'com.getkeepsafe.dexcount'
    dexcount {
        format = "json"
        includeClassCount = true
        includeFieldCount = true
        includeTotalMethodCount = true
    }
}

